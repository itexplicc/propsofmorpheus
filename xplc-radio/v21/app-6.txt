function renderTuner(){const count=(field,val)=>all.filter(t=>field==='languages'?t.languages.includes(val):t[field]===val).length;$('#langChoices').innerHTML=LANGS.filter(x=>count('languages',x)).map(x=>renderChoice(lab(x),x,'languages',count('languages',x),draft.languages.includes(x))).join('');$('#typeChoices').innerHTML=TYPES.filter(x=>count('type',x)).map(x=>renderChoice(lab(x),x,'types',count('type',x),draft.types.includes(x))).join('');$('#genreChoices').innerHTML=GENRES.filter(x=>count('genre',x)).map(x=>renderChoice(lab(x),x,'genres',count('genre',x),draft.genres.includes(x))).join('');const clean=all.filter(t=>!t.explicit).length;$('#cleanChoices').innerHTML=renderChoice(tr('clean'),'clean','clean',clean,draft.cleanMode);$('#draftSummary').textContent=summary(draft);$$('[data-group]').forEach(b=>b.onclick=()=>{const g=b.dataset.group,v=b.dataset.value;if(g==='clean')draft.cleanMode=!draft.cleanMode;else{const a=draft[g],i=a.indexOf(v);i>=0?a.splice(i,1):a.push(v)}renderTuner()})}
function applyPrefs(resync=true){choosePool();updateCounts();if(!pool.length){$('#title').textContent=tr('none');$('#artist').textContent=tr('retune');$('#queue').innerHTML='<div class="empty">Try another tune.</div>';activeAudio().pause();setPlaying(false);return}const info=scheduleInfo();renderTrack(info);if(resync&&wantedPlaying)hardJoin(info,true);else prepareNext(info)}
function localizeStatic(){$$('[data-t]').forEach(e=>e.textContent=tr(e.dataset.t));if($('#uiBtn'))$('#uiBtn').textContent=prefs.ui==='si'?'EN':'සිංහල'}
function clock(){const x=$('#clock');if(x)x.textContent=new Date().toLocaleTimeString('en-LK',{timeZone:'Asia/Colombo',hour:'2-digit',minute:'2-digit',hour12:false})+' LKA'}
function toast(msg){const x=$('#toast');x.textContent=msg;x.classList.add('show');clearTimeout(x._t);x._t=setTimeout(()=>x.classList.remove('show'),2000)}
async function pubApi(action,body={}){const r=await fetch(CFG.publicFn,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action,listenerKey,...body})}),j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error||'Unavailable');return j}
async function loadReactions(){try{const j=await pubApi('stats',{trackIds:all.map(t=>t.id)});likes=j.likes||{};dislikes=j.dislikes||{};myReaction=j.reactions||{};paintReaction()}catch{}}
function paintReaction(){const t=lastInfo?.track;if(!t)return;const r=myReaction[t.id]||'',l=$('#likeBtn'),d=$('#dislikeBtn');if(l){l.classList.toggle('active',r==='like');l.innerHTML=(r==='like'?'♥':'♡')+`<span class="count" id="likeCount">${likes[t.id]||0}</span>`}if(d)d.classList.toggle('active',r==='dislike');$('#immersiveLike').textContent=r==='like'?'♥':'♡';$('#immersiveDislike').style.color=r==='dislike'?'#ff817b':'#fff'}
async function react(kind){const t=lastInfo?.track;if(!t)return;try{const j=await pubApi('react',{trackId:t.id,reaction:kind});myReaction[t.id]=j.reaction||'';likes[t.id]=j.likes||0;dislikes[t.id]=j.dislikes||0;paintReaction();navigator.vibrate?.(22);if(kind==='dislike'&&j.reaction==='dislike')toast('Noted — this helps shape the station')}catch{toast('Could not save reaction')}}
async function share(){const t=lastInfo?.track,d={title:'XPLC Radio',text:t?`Listening to “${t.title}” by XPLC — live on XPLC Radio.`:'XPLC Radio — live now.',url:location.origin};if(navigator.share)navigator.share(d).catch(()=>{});else navigator.clipboard?.writeText(location.origin).then(()=>toast('Station link copied'))}
async function load(){const[q,s]=await Promise.all([db.from('explic_radio_tracks').select('id,title,artist,album,audio_path,cover_path,lyrics,timed_lyrics,duration_seconds,genre,languages,song_type,is_explicit,sort_order').eq('enabled',true).order('sort_order'),db.from('explic_radio_settings').select('*').eq('id','main').maybeSingle()]);if(q.error)throw q.error;if(s.data)settings=s.data;all=(q.data||[]).map(t=>({id:t.id,title:t.title,artist:t.artist||'XPLC',album:t.album||'',src:fileUrl(t.audio_path),cover:t.cover_path?fileUrl(t.cover_path):'',lyrics:t.lyrics||'',timed:Array.isArray(t.timed_lyrics)?t.timed_lyrics:[],duration:+t.duration_seconds,genre:t.genre||'Other',type:t.song_type||'Other',languages:Array.isArray(t.languages)&&t.languages.length?t.languages:['English'],explicit:!!t.is_explicit}));prefs.languages=prefs.languages.filter(x=>LANGS.includes(x)&&all.some(t=>t.languages.includes(x)));prefs.types=prefs.types.filter(x=>TYPES.includes(x)&&all.some(t=>t.type===x));prefs.genres=prefs.genres.filter(x=>GENRES.includes(x)&&all.some(t=>t.genre===x));savePrefs();renderShell();choosePool();updateCounts();const info=scheduleInfo();if(info){renderTrack(info);await hardJoin(info,false)}await loadReactions();clock();setInterval(clock,30000);setInterval(tick,250)}
