let listenerKey=localStorage.getItem('xplc_radio_listener');if(!listenerKey){listenerKey=(crypto.randomUUID?.()||Math.random().toString(36).slice(2)+Date.now()).replaceAll('-','');localStorage.setItem('xplc_radio_listener',listenerKey)}
let audioCtx=null,analyser=null,gains=[],sources=[],freq=null,bassMean=0,pulse=0;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=s=>{s=Math.max(0,Math.floor(s||0));return `${Math.floor(s/60)}:${String(s%60).padStart(2,'0')}`};
const tr=k=>(T[prefs.ui]||T.en)[k]||k,lab=v=>prefs.ui==='si'?(SI_LABEL[v]||v):v;
function loadPrefs(){let x={};try{x=JSON.parse(localStorage.getItem('xplc_radio_prefs')||'{}')}catch{}return{languages:Array.isArray(x.languages)?x.languages:[],genres:Array.isArray(x.genres)?x.genres:[],types:Array.isArray(x.types)?x.types:[],cleanMode:!!x.cleanMode,ui:x.ui==='si'?'si':'en'}}
function savePrefs(){localStorage.setItem('xplc_radio_prefs',JSON.stringify(prefs))}
function fileUrl(path){return CFG.url+'/storage/v1/object/public/'+CFG.bucket+'/'+path.split('/').map(encodeURIComponent).join('/')}
function hash(s){let h=2166136261>>>0;for(const c of s){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0}
function rand(seed){return()=>{seed|=0;seed=seed+0x6D2B79F5|0;let t=Math.imul(seed^seed>>>15,1|seed);t=t+Math.imul(t^t>>>7,61|t)^t;return((t^t>>>14)>>>0)/4294967296}}
function normTitle(t){return String(t?.title||'').normalize('NFKC').trim().toLowerCase().replace(/\s+/g,' ')}
function dedupeKey(t){return `${normTitle(t)}|${Math.round(+t.duration||0)}`}
function dayKey(){const parts=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Colombo',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date()),o={};for(const p of parts)if(p.type!=='literal')o[p.type]=p.value;return`${o.year}-${o.month}-${o.day}`}
function selectedKey(){return`L:${[...prefs.languages].sort()}|T:${[...prefs.types].sort()}|G:${[...prefs.genres].sort()}|C:${prefs.cleanMode?1:0}`}
function intersects(a,b){return !b.length||a.some(x=>b.includes(x))}
function choosePool(){smartAdded=0;let base=prefs.cleanMode?all.filter(t=>!t.explicit):[...all];if(prefs.languages.length)base=base.filter(t=>intersects(t.languages,prefs.languages));const unique=[],seen=new Set();for(const t of base){const k=dedupeKey(t);if(!seen.has(k)){seen.add(k);unique.push(t)}}base=unique;if(!prefs.types.length&&!prefs.genres.length){pool=base;return}let strict=base.filter(t=>(!prefs.types.length||prefs.types.includes(t.type))&&(!prefs.genres.length||prefs.genres.includes(t.genre)));if(strict.length>=4||strict.length===base.length){pool=strict;return}const ids=new Set(strict.map(t=>t.id)),rest=base.filter(t=>!ids.has(t.id)).map(t=>{let score=0;if(prefs.types.includes(t.type))score+=3;if(prefs.genres.includes(t.genre))score+=3;score+=(hash(t.id+selectedKey())%1000)/1e6;return{t,score}}).sort((a,b)=>b.score-a.score);const wanted=Math.min(4,base.length),add=rest.slice(0,Math.max(0,wanted-strict.length)).map(x=>x.t);smartAdded=add.length;pool=[...strict,...add];if(!pool.length)pool=base.slice(0,Math.min(4,base.length))}
function order(){const a=[...pool];if(!settings.shuffle_enabled||a.length<2)return a;const r=rand(hash(`${settings.shuffle_seed}|${dayKey()}|${selectedKey()}`));for(let i=a.length-1;i>0;i--){const j=Math.floor(r()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}
function scheduleInfo(now=Date.now()){if(!pool.length)return null;const seq=order(),total=seq.reduce((s,t)=>s+t.duration,0);if(!total)return null;let raw=(now-epoch)/1000,x=((raw%total)+total)%total,cycle=Math.floor(raw/total);for(let i=0;i<seq.length;i++){if(x<seq[i].duration)return{seq,total,cycle,index:i,track:seq[i],elapsed:x,remaining:seq[i].duration-x};x-=seq[i].duration}return null}
function queueAfter(info,n=3){if(!info||pool.length<2)return[];const out=[],seen=new Set([dedupeKey(info.track)]);let k=info.index+1;while(out.length<n&&out.length<pool.length-1){if(k>=info.seq.length)k=0;const t=info.seq[k++],key=dedupeKey(t);if(!seen.has(key)){seen.add(key);out.push(t)}}return out}
function summary(p=prefs){let a=[];if(p.languages.length)a.push(p.languages.map(lab).join(' + '));if(p.types.length)a.push(p.types.map(lab).join(' + '));if(p.genres.length)a.push(p.genres.map(lab).join(' + '));if(p.cleanMode)a.push(prefs.ui==='si'?'✓ CLEAN':'✓ CLEAN');return a.length?a.join(' · '):tr('everything')}
function markSinhala(el,text){if(!el)return;const si=/[\u0D80-\u0DFF]/.test(text||'');el.classList.toggle('siTitle',si);if(si)el.lang='si';else el.removeAttribute('lang')}
