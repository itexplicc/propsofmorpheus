/* XPLC Radio station-ID scheduling layer — loaded inside the v21 runtime IIFE */
let stationIds=[],stationCfg={enabled:true,minGap:8,maxGap:12};
const stationListUrl=CFG.url+'/functions/v1/explic-radio-station-list';
const baseRenderShell=renderShell,baseRenderTrack=renderTrack,baseRenderQueue=renderQueue,baseRenderLyrics=renderLyrics,baseUpdateLyric=updateLyric,basePaintReaction=paintReaction,baseReact=react;
renderShell=function(){baseRenderShell();const f=$('.footer');if(f)f.innerHTML='BROADCASTING CONTINUOUSLY · SRI LANKA TIME<br>NO EXTERNAL ADS · YOUR XPLC SIGNAL'};
function sidDeck(r){const a=[...stationIds];for(let i=a.length-1;i>0;i--){const j=Math.floor(r()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}
function stationProgram(){const songs=order();if(!songs.length)return[];if(!stationCfg.enabled||!stationIds.length)return songs;const min=Math.max(5,Math.min(30,Math.round(+stationCfg.minGap||8))),max=Math.max(min,Math.min(30,Math.round(+stationCfg.maxGap||12))),r=rand(hash(`${settings.shuffle_seed}|${dayKey()}|${selectedKey()}|XPLC-STATION-ID-V1`));const target=Math.max(songs.length,max*stationIds.length);let songPos=0,deck=sidDeck(r),adPos=0,lastAd=null;const out=[];while(songPos<target){const gap=min+Math.floor(r()*(max-min+1)),take=Math.min(gap,target-songPos);for(let j=0;j<take;j++)out.push(songs[(songPos+j)%songs.length]);songPos+=take;if(take<min&&songPos>=target)break;if(adPos>=deck.length){let next=sidDeck(r);if(next.length>1&&lastAd&&next[0].id===lastAd.id)[next[0],next[1]]=[next[1],next[0]];deck=next;adPos=0}const ad=deck[adPos++];lastAd=ad;out.push(ad)}return out}
scheduleInfo=function(now=Date.now()){if(!pool.length)return null;const seq=stationProgram(),total=seq.reduce((s,t)=>s+t.duration,0);if(!total)return null;const raw=(now-epoch)/1000,cycle=Math.floor(raw/total);let x=((raw%total)+total)%total;for(let i=0;i<seq.length;i++){if(x<seq[i].duration)return{seq,total,cycle,index:i,track:seq[i],elapsed:x,remaining:seq[i].duration-x};x-=seq[i].duration}return null};
queueAfter=function(info,n=3){if(!info||info.seq.length<2)return[];const out=[];let k=info.index+1;while(out.length<n&&out.length<info.seq.length-1){if(k>=info.seq.length)k=0;out.push(info.seq[k++])}return out};
function sidControls(on){for(const id of ['#likeBtn','#dislikeBtn','#immersiveLike','#immersiveDislike']){const b=$(id);if(!b)continue;b.disabled=on;b.style.opacity=on?'.28':'';b.style.pointerEvents=on?'none':''}}
renderQueue=function(info){const q=queueAfter(info,3),box=$('#queue');if(!q.length){box.innerHTML='<div class="empty">This signal has one matching track right now.</div>';return}box.innerHTML=q.map(t=>t.stationId?`<article class="q"><img src="/icon-192.png"><div><b class="${/[\u0D80-\u0DFF]/.test(t.title)?'siTitle':''}">${esc(t.title)}</b><small>XPLC RADIO · STATION ID · ${esc(lab(t.language))}</small></div><time>${fmt(t.duration)}</time></article>`:`<article class="q"><img src="${esc(t.cover||'/icon-192.png')}"><div><b class="${/[\u0D80-\u0DFF]/.test(t.title)?'siTitle':''}">${esc(t.title)}</b><small>${esc(t.artist||'XPLC')} · ${esc([lab(t.type),lab(t.genre)].filter(x=>x&&x!=='Other'&&x!=='වෙනත්').join(' • ')||'LIVE')}</small></div><time>${fmt(t.duration)}</time></article>`).join('')};
renderLyrics=function(t){if(!t?.stationId)return baseRenderLyrics(t);$('#lyricsTitle').textContent=t.title;markSinhala($('#lyricsTitle'),t.title);$('#lyricsArtist').textContent='XPLC Radio · Station ID';lastLyric=-2;$('#lyricsBody').innerHTML=t.lyrics?`<div class="plainLyrics">${esc(t.lyrics)}</div>`:'<div class="empty">Station ID</div>'};
updateLyric=function(info){if(!info?.track?.stationId)return baseUpdateLyric(info);$('#liveLyric').textContent='';$('#immersiveLyric').textContent=''};
paintReaction=function(){if(lastInfo?.track?.stationId){sidControls(true);return}sidControls(false);return basePaintReaction()};
react=async function(kind){if(lastInfo?.track?.stationId)return;return baseReact(kind)};
renderTrack=function(info){if(!info?.track?.stationId){sidControls(false);return baseRenderTrack(info)}lastInfo=info;const t=info.track;sidControls(true);$('#title').textContent=t.title;markSinhala($('#title'),t.title);$('#artist').textContent='XPLC Radio · Station ID';const img=$('#coverImg'),fb=$('#fallback');img.classList.add('hidden');fb.classList.remove('hidden');$('#progress').style.width=`${Math.min(100,info.elapsed/t.duration*100)}%`;$('#elapsed').textContent=fmt(info.elapsed);$('#remain').textContent='−'+fmt(info.remaining);$('#meta').innerHTML=`<span class="tag smart">STATION ID</span><span class="tag">${esc(lab(t.language))}</span>`;renderQueue(info);renderLyrics(t);updateLyric(info);mediaMeta({title:t.title,artist:'XPLC Radio',album:'One Tune · One Network · One Explic',cover:''});$('#immersiveArt').src='/icon-512.png';$('#immersiveTitle').textContent=t.title;markSinhala($('#immersiveTitle'),t.title);$('#immersiveArtist').textContent='XPLC Radio';$('#immersiveMeta').textContent=`STATION ID • ${lab(t.language)}`;$('#immersiveBg').style.backgroundImage='none'};
load=async function(){const[q,s,si]=await Promise.all([db.from('explic_radio_tracks').select('id,title,artist,album,audio_path,cover_path,lyrics,timed_lyrics,duration_seconds,genre,languages,song_type,is_explicit,sort_order').eq('enabled',true).order('sort_order'),db.from('explic_radio_settings').select('*').eq('id','main').maybeSingle(),fetch(stationListUrl,{cache:'no-cache'}).then(r=>r.ok?r.json():({ids:[],settings:{}})).catch(()=>({ids:[],settings:{}}))]);if(q.error)throw q.error;if(s.data)settings=s.data;stationCfg={enabled:si.settings?.enabled!==false,minGap:+si.settings?.minGap||8,maxGap:+si.settings?.maxGap||12};stationIds=(si.ids||[]).filter(x=>x?.src&&+x.duration>0).map(x=>({id:x.id,dbId:x.dbId,title:x.title||'XPLC Radio',artist:'XPLC Radio',album:'XPLC Radio',src:x.src,cover:'',lyrics:x.transcript||'',timed:[],duration:+x.duration,genre:'Station ID',type:'Station ID',languages:[x.language||'English'],language:x.language||'English',explicit:false,stationId:true,variant:x.variant||''}));all=(q.data||[]).map(t=>({id:t.id,title:t.title,artist:t.artist||'XPLC',album:t.album||'',src:fileUrl(t.audio_path),cover:t.cover_path?fileUrl(t.cover_path):'',lyrics:t.lyrics||'',timed:Array.isArray(t.timed_lyrics)?t.timed_lyrics:[],duration:+t.duration_seconds,genre:t.genre||'Other',type:t.song_type||'Other',languages:Array.isArray(t.languages)&&t.languages.length?t.languages:['English'],explicit:!!t.is_explicit}));prefs.languages=prefs.languages.filter(x=>LANGS.includes(x)&&all.some(t=>t.languages.includes(x)));prefs.types=prefs.types.filter(x=>TYPES.includes(x)&&all.some(t=>t.type===x));prefs.genres=prefs.genres.filter(x=>GENRES.includes(x)&&all.some(t=>t.genre===x));savePrefs();renderShell();choosePool();updateCounts();const info=scheduleInfo();if(info){renderTrack(info);await hardJoin(info,false)}await loadReactions();clock();setInterval(clock,30000);setInterval(tick,250)};
