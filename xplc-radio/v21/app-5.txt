function nominalRate(el,t){const d=el.duration;if(!Number.isFinite(d)||d<=0||!t?.duration)return 1;return Math.max(.995,Math.min(1.005,d/t.duration))}
async function prepareNext(info){const n=queueAfter(info,1)[0],el=standbyAudio();if(!n)return;standbyTrackId=n.id;if(el.dataset.trackId===n.id&&el.readyState>=2)return;el.pause();if(gains[1-activeIndex])gains[1-activeIndex].gain.value=0;await loadInto(el,n,0,false)}
async function hardJoin(info,play=true){if(!info)return;transitioning=true;const a=activeAudio(),s=standbyAudio();s.pause();if(gains[1-activeIndex])gains[1-activeIndex].gain.value=0;await loadInto(a,info.track,info.elapsed,false);activeTrackId=info.track.id;renderTrack(info);if(play){await armAudio();try{await a.play();setPlaying(true)}catch{setPlaying(false);toast(tr('audioBlocked'))}}else setPlaying(false);a.playbackRate=nominalRate(a,info.track);transitioning=false;prepareNext(info)}
async function handoff(info){if(transitioning||!wantedPlaying)return;transitioning=true;const old=activeAudio(),next=standbyAudio(),nextIndex=1-activeIndex;let ready=next.dataset.trackId===info.track.id&&next.readyState>=2;if(!ready)ready=await loadInto(next,info.track,0,false);let startAt=info.elapsed<=.9?0:info.elapsed;try{next.currentTime=startAt}catch{}next.playbackRate=nominalRate(next,info.track);await armAudio();let ok=false;try{await next.play();ok=true}catch{}if(!ok){transitioning=false;return hardJoin(info,true)}if(gains[nextIndex]&&gains[activeIndex]&&audioCtx){const now=audioCtx.currentTime;gains[nextIndex].gain.cancelScheduledValues(now);gains[activeIndex].gain.cancelScheduledValues(now);gains[nextIndex].gain.setValueAtTime(0,now);gains[activeIndex].gain.setValueAtTime(gains[activeIndex].gain.value,now);gains[nextIndex].gain.linearRampToValueAtTime(1,now+.075);gains[activeIndex].gain.linearRampToValueAtTime(0,now+.075)}else{next.volume=1}activeIndex=nextIndex;activeTrackId=info.track.id;renderTrack(info);next.playbackRate=nominalRate(next,info.track)*(info.elapsed>.12&&info.elapsed<.9?1.035:1);setTimeout(()=>{old.pause();old.playbackRate=1;if(gains[1-activeIndex])gains[1-activeIndex].gain.value=0;transitioning=false;prepareNext(scheduleInfo())},95)}
function driftCorrect(info){if(!wantedPlaying||transitioning)return;const a=activeAudio();if(a.dataset.trackId!==info.track.id)return;const base=nominalRate(a,info.track),drift=(a.currentTime||0)-info.elapsed,abs=Math.abs(drift);if(abs>2.2&&Date.now()-lastHardSync>6000){lastHardSync=Date.now();try{a.currentTime=info.elapsed}catch{}a.playbackRate=base;return}if(abs<.08)a.playbackRate=base;else if(drift<0)a.playbackRate=base*(abs>.7?1.035:abs>.3?1.02:1.01);else a.playbackRate=base*(abs>.7?.965:abs>.3?.98:.99)}
async function syncNow(force=false){const info=scheduleInfo();if(!info)return;if(force||activeAudio().dataset.trackId!==info.track.id)return hardJoin(info,wantedPlaying);try{activeAudio().currentTime=info.elapsed}catch{}activeAudio().playbackRate=nominalRate(activeAudio(),info.track);renderTrack(info)}
function tick(){const info=scheduleInfo();if(!info)return;if(!lastInfo||lastInfo.track.id!==info.track.id){renderTrack(info);if(wantedPlaying)handoff(info);else hardJoin(info,false)}else{lastInfo=info;$('#progress').style.width=`${Math.min(100,info.elapsed/info.track.duration*100)}%`;$('#elapsed').textContent=fmt(info.elapsed);$('#remain').textContent='−'+fmt(info.remaining);updateLyric(info);driftCorrect(info)}if(info.remaining<18)prepareNext(info)}
async function togglePlay(){await armAudio();if(wantedPlaying){activeAudio().pause();setPlaying(false)}else{setPlaying(true);const info=scheduleInfo();if(activeAudio().dataset.trackId!==info?.track.id)await hardJoin(info,true);else try{await activeAudio().play()}catch{setPlaying(false);toast(tr('audioBlocked'))}}}
function openPanel(id){const p=$('#'+id);p.classList.add('open');p.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}function closePanel(id){const p=$('#'+id);p.classList.remove('open');p.setAttribute('aria-hidden','true');document.body.style.overflow=''}
function bindMain(){$('#playBtn').onclick=togglePlay;$('#likeBtn').onclick=()=>react('like');$('#dislikeBtn').onclick=()=>react('dislike');$('#syncBtn').onclick=()=>syncNow(true);$('#lyricsBtn').onclick=()=>{const i=scheduleInfo();if(i){renderLyrics(i.track);updateLyric(i)}openPanel('lyricsPanel')};$('#shareBtn').onclick=share;$('#shareTop').onclick=share;$('#installBtn').onclick=()=>openPanel('installPanel');$('#tuneBtn').onclick=()=>{draft=structuredClone(prefs);renderTuner();openPanel('tunePanel')};$('#uiBtn').onclick=()=>{prefs.ui=prefs.ui==='si'?'en':'si';savePrefs();renderShell();applyPrefs(false)};$('#immersiveBtn').onclick=()=>{const i=scheduleInfo();if(i)renderTrack(i);$('#immersive').classList.add('open');$('#immersive').setAttribute('aria-hidden','false');document.body.style.overflow='hidden';document.documentElement.requestFullscreen?.().catch(()=>{})};$('#songCount').onclick=()=>{draft=structuredClone(prefs);renderTuner();openPanel('tunePanel')}}
function renderChoice(label,value,group,count,active){return`<button class="choice ${active?'active':''}" data-group="${group}" data-value="${esc(value)}">${esc(label)}<small>${count}</small></button>`}
