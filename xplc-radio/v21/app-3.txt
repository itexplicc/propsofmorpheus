function renderShell(){app.innerHTML=`<header class="top"><div class="brand"><span class="brandMark"></span><span class="brandText"><b>EXPLIC</b><small>RADIO</small></span></div><div class="topTools"><button class="chip countChip" id="songCount">0 SONGS</button><button class="chip" id="uiBtn">${prefs.ui==='si'?'EN':'සිංහල'}</button><button class="chip" id="tuneBtn">◉ <span>${tr('tune')}</span></button><button class="chip" id="installBtn">＋ <span>${tr('install')}</span></button><button class="chip" id="shareTop">↗</button></div></header><section class="hero"><div class="liveLine"><i></i><b>LIVE</b><span>${tr('live')}</span></div><div class="art"><canvas id="mainWave" class="artCanvas"></canvas><div class="cover"><img id="coverImg" class="hidden" alt=""><div class="fallback" id="fallback"><div><b>XPLC</b><small>RADIO</small></div></div></div></div><div class="nowLabel">${tr('now')}</div><h1 id="title" class="songTitle">Tuning the signal…</h1><div id="artist" class="artist">XPLC</div><div id="liveLyric" class="liveLyric"></div><div id="meta" class="meta"></div><div class="timeline"><div class="trackLine"><i id="progress"></i></div><div class="times"><span id="elapsed">0:00</span><button id="syncBtn" class="sync">${tr('sync')}</button><span id="remain">−0:00</span></div></div><div class="controls"><button class="round" id="dislikeBtn">👎</button><button class="round play" id="playBtn">▶</button><button class="round" id="likeBtn">♡<span class="count" id="likeCount">0</span></button></div><div class="secondary"><button id="lyricsBtn">${tr('lyrics')}</button><button id="shareBtn">${tr('share')}</button><button id="immersiveBtn">${tr('immersive')}</button></div></section><section class="card"><div class="cardHead"><div><small id="signalLabel">${tr('signal')} · ${esc(summary())}</small><h2>${tr('up')}</h2></div><div class="clock" id="clock">--:--</div></div><div id="queue" class="queue"></div></section><footer class="footer">BROADCASTING CONTINUOUSLY · SRI LANKA TIME<br>NO ACCOUNTS · NO ADS · YOUR XPLC SIGNAL</footer>`;bindMain();localizeStatic()}
function updateCounts(){const total=all.length,signal=pool.length;$('#songCount').textContent=`${total} ${total===1?tr('song'):tr('songs')}`;$('#signalLabel').textContent=`${tr('signal')} · ${signal} ${tr('inSignal')} · ${summary()}`}
function renderTrack(info){if(!info)return;lastInfo=info;const t=info.track;$('#title').textContent=t.title;markSinhala($('#title'),t.title);$('#artist').textContent=t.artist||'XPLC';const img=$('#coverImg'),fb=$('#fallback');if(t.cover){img.src=t.cover;img.classList.remove('hidden');fb.classList.add('hidden');$('#ambient').style.background=`radial-gradient(circle at 50% 22%,rgba(255,70,50,.08),transparent 30%),linear-gradient(#070707,#050505)`}else{img.classList.add('hidden');fb.classList.remove('hidden')}$('#progress').style.width=`${Math.min(100,info.elapsed/t.duration*100)}%`;$('#elapsed').textContent=fmt(info.elapsed);$('#remain').textContent='−'+fmt(info.remaining);$('#meta').innerHTML=(t.type!=='Other'?`<span class="tag">${esc(lab(t.type))}</span>`:'')+(t.genre!=='Other'?`<span class="tag">${esc(lab(t.genre))}</span>`:'')+t.languages.map(x=>`<span class="tag">${esc(lab(x))}</span>`).join('')+(settings.shuffle_enabled?'<span class="tag">⤨ SHUFFLED</span>':'')+(smartAdded?`<span class="tag smart">SMART +${smartAdded}</span>`:'')+(prefs.cleanMode?'<span class="tag smart">✓ CLEAN</span>':'');renderQueue(info);renderLyrics(t);updateLyric(info);paintReaction();mediaMeta(t);$('#immersiveArt').src=t.cover||'/icon-512.png';$('#immersiveTitle').textContent=t.title;markSinhala($('#immersiveTitle'),t.title);$('#immersiveArtist').textContent=t.artist||'XPLC';$('#immersiveMeta').textContent=[lab(t.type),lab(t.genre),...t.languages.map(lab)].filter(x=>x&&x!=='Other'&&x!=='වෙනත්').join(' • ');$('#immersiveBg').style.backgroundImage=t.cover?`url("${t.cover.replaceAll('"','%22')}")`:'none'}
function renderQueue(info){const q=queueAfter(info,3),box=$('#queue');if(!q.length){box.innerHTML='<div class="empty">This signal has one matching track right now.</div>';return}box.innerHTML=q.map(t=>`<article class="q"><img src="${esc(t.cover||'/icon-192.png')}"><div><b class="${/[\u0D80-\u0DFF]/.test(t.title)?'siTitle':''}">${esc(t.title)}</b><small>${esc(t.artist||'XPLC')} · ${esc([lab(t.type),lab(t.genre)].filter(x=>x&&x!=='Other'&&x!=='වෙනත්').join(' • ')||'LIVE')}</small></div><time>${fmt(t.duration)}</time></article>`).join('')}
function renderLyrics(t){$('#lyricsTitle').textContent=t.title;markSinhala($('#lyricsTitle'),t.title);$('#lyricsArtist').textContent=t.artist||'XPLC';const b=$('#lyricsBody');lastLyric=-2;if(t.timed.length){b.innerHTML=t.timed.map((x,i)=>`<div class="lyLine" data-li="${i}">${esc(x.text)}</div>`).join('')}else if(t.lyrics){b.innerHTML=`<div class="plainLyrics">${esc(t.lyrics)}</div>`}else b.innerHTML='<div class="empty">Lyrics haven’t been added for this track yet.</div>'}
function lyricIndex(t,ms){if(!t?.timed?.length)return-1;let lo=0,hi=t.timed.length-1,ans=-1;while(lo<=hi){const m=(lo+hi)>>1;if(t.timed[m].ms<=ms){ans=m;lo=m+1}else hi=m-1}return ans}
