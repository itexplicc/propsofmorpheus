function updateLyric(info){if(!info)return;const idx=lyricIndex(info.track,info.elapsed*1000),text=idx>=0?info.track.timed[idx].text:'';$('#liveLyric').textContent=text;$('#immersiveLyric').textContent=text;if(idx===lastLyric)return;lastLyric=idx;$$('.lyLine').forEach((e,i)=>{e.classList.toggle('active',i===idx);e.classList.toggle('near',i!==idx&&Math.abs(i-idx)<=2)});if(idx>=0&&$('#lyricsPanel').classList.contains('open')){const active=$(`.lyLine[data-li="${idx}"]`),sc=$('#lyricsPanel .panelScroll');if(active&&sc){const ar=active.getBoundingClientRect(),sr=sc.getBoundingClientRect();if(ar.top<sr.top+100||ar.bottom>sr.bottom-100)sc.scrollTo({top:Math.max(0,sc.scrollTop+(ar.top-sr.top)-sc.clientHeight*.4),behavior:'smooth'})}}}
function mediaMeta(t){if(!('mediaSession'in navigator))return;try{navigator.mediaSession.metadata=new MediaMetadata({title:t.title,artist:t.artist||'XPLC',album:t.album||'XPLC Radio',artwork:[{src:t.cover||location.origin+'/icon-512.png',sizes:'512x512'}]})}catch{}}
function setPlaying(v){wantedPlaying=v;document.body.classList.toggle('playing',v);const ch=v?'Ⅱ':'▶';if($('#playBtn'))$('#playBtn').textContent=ch;if($('#immersivePlay'))$('#immersivePlay').textContent=ch}
async function initAudioGraph(){if(audioCtx)return;const AC=window.AudioContext||window.webkitAudioContext;if(!AC)return;audioCtx=new AC();analyser=audioCtx.createAnalyser();analyser.fftSize=2048;analyser.smoothingTimeConstant=.66;analyser.minDecibels=-92;analyser.maxDecibels=-8;freq=new Uint8Array(analyser.frequencyBinCount);for(let i=0;i<2;i++){sources[i]=audioCtx.createMediaElementSource(audioEls[i]);gains[i]=audioCtx.createGain();gains[i].gain.value=i===activeIndex?1:0;sources[i].connect(gains[i]);gains[i].connect(analyser)}analyser.connect(audioCtx.destination);requestAnimationFrame(drawVisuals)}
async function armAudio(){try{await initAudioGraph();if(audioCtx?.state==='suspended')await audioCtx.resume()}catch(e){console.warn('audio graph',e)}}
function band(a,b){if(!analyser||!freq||!audioCtx)return 0;const hz=audioCtx.sampleRate/analyser.fftSize,s=Math.max(1,Math.floor(a/hz)),e=Math.min(freq.length-1,Math.ceil(b/hz));let sum=0,n=0;for(let i=s;i<=e;i++){sum+=freq[i];n++}return n?sum/(n*255):0}
function fitCanvas(c){const r=c.getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1),w=Math.max(1,Math.round(r.width*d)),h=Math.max(1,Math.round(r.height*d));if(c.width!==w||c.height!==h){c.width=w;c.height=h}const x=c.getContext('2d');x.setTransform(d,0,0,d,0,0);return{x,w:r.width,h:r.height}}
function paintWave(c,ts,bass,lm,hm,trn){if(!c)return;const{x,w,h}=fitCanvas(c),cx=w/2,cy=h/2;x.clearRect(0,0,w,h);const active=wantedPlaying&&!audioEls[activeIndex].paused;bassMean=bassMean*.90+bass*.10;const transient=Math.max(0,bass-bassMean);pulse=Math.max(transient*3.8,bass*.16,pulse*.80);const seg=92,base=Math.min(w,h)*.385+bass*7+pulse*15;x.save();x.globalCompositeOperation='lighter';x.lineCap='round';for(let i=0;i<seg;i++){let amp=.02;if(active&&analyser&&freq){const ratio=i/(seg-1),f=45+Math.pow(ratio,1.55)*5200,hz=audioCtx.sampleRate/analyser.fftSize,bin=Math.max(1,Math.min(freq.length-1,Math.round(f/hz)));amp=(freq[bin]||0)/255}const ang=i/seg*Math.PI*2-Math.PI/2,sh=amp*.78+bass*.22,len=3+sh*29+pulse*7,r0=base,r1=r0+len,hue=(i/seg*330+(active?ts:0)/38)%360,alpha=active?.30+sh*.70:.13;x.beginPath();x.moveTo(cx+Math.cos(ang)*r0,cy+Math.sin(ang)*r0);x.lineTo(cx+Math.cos(ang)*r1,cy+Math.sin(ang)*r1);x.strokeStyle=`hsla(${hue},100%,66%,${alpha})`;x.lineWidth=1+sh*2.6+pulse*.6;x.shadowBlur=active?8+sh*18+pulse*12:5;x.shadowColor=`hsla(${hue},100%,60%,.9)`;x.stroke()}const energy=bass*.5+lm*.25+hm*.17+trn*.08,hh=(active?ts:0)/31%360;x.beginPath();x.arc(cx,cy,base+1.5,0,Math.PI*2);x.strokeStyle=`hsla(${hh},100%,65%,${active?.15+energy*.50:.11})`;x.lineWidth=1.2+pulse*3+energy;x.shadowBlur=active?13+pulse*30:7;x.shadowColor=`hsla(${hh},100%,61%,.92)`;x.stroke();x.restore()}
function drawVisuals(ts=0){requestAnimationFrame(drawVisuals);if(analyser&&freq&&wantedPlaying&&!audioEls[activeIndex].paused)analyser.getByteFrequencyData(freq);const bass=band(42,190),lm=band(190,700),hm=band(700,2400),trn=band(2400,6500);paintWave($('#mainWave'),ts,bass,lm,hm,trn);paintWave($('#immersiveWave'),ts,bass,lm,hm,trn)}
function activeAudio(){return audioEls[activeIndex]}function standbyAudio(){return audioEls[1-activeIndex]}
function waitMedia(el,timeout=5000){return new Promise(resolve=>{if(el.readyState>=3)return resolve(true);let done=()=>{cleanup();resolve(el.readyState>=2)},cleanup=()=>{el.removeEventListener('canplay',done);el.removeEventListener('loadeddata',done);el.removeEventListener('error',done);clearTimeout(tm)},tm=setTimeout(()=>{cleanup();resolve(el.readyState>=2)},timeout);el.addEventListener('canplay',done,{once:true});el.addEventListener('loadeddata',done,{once:true});el.addEventListener('error',done,{once:true})})}
async function loadInto(el,t,seek=0,auto=false){if(!t)return false;el.crossOrigin='anonymous';const same=el.dataset.trackId===t.id;if(!same){el.dataset.trackId=t.id;el.src=t.src;el.preload='auto';el.load()}await waitMedia(el);try{if(Math.abs((el.currentTime||0)-seek)>.15)el.currentTime=Math.max(0,Math.min(seek,(Number.isFinite(el.duration)?el.duration:t.duration)-.08))}catch{}if(auto){try{await el.play();return true}catch{return false}}return el.readyState>=2}
